/**
 * The list of languages the site is built in.
 *
 * Adding a language is this array plus a translation file in src/i18n/ui.ts
 * and a content folder per collection. No routing changes.
 */
export const LANGS = ['en', 'fr'];

/** Used when the browser gives us nothing useful to go on. */
export const DEFAULT_LANG = 'en';
